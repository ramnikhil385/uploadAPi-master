spring:
  application:
    name: ${APP_NAME:uploadapi} 
  profiles:
    active: ${BATCH_ENV:dev}
  cloud:
    config:
      label: ${BATCH_HOST:apsrs7230}
      uri: http://configserver-doc360-prd.origin-ctc-core.optum.com

      
